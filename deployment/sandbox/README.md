# Deployment Sandbox Profiles

This directory contains deployment-time isolation contracts and validation metadata.

Do not treat these files as substitutes for OS-enforced sandboxing. Platform-specific policies must be generated/installed by the packaging pipeline and validated on the target operating system.

A release is not enterprise-certified until each supported target has an attached, reproducible isolation-policy artifact and passing security evidence.
