# Never put secrets in VITE_* variables. These values are public client configuration only.
VITE_HUI_WS_URL=wss://hui.example.com/v1/events
VITE_HUI_ALLOWED_ORIGIN=https://hui.example.com
# Development only; never enable this for public deployments.
VITE_HUI_ALLOW_INSECURE_LOCALHOST=false
